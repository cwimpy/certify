% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{make_certificates}
\alias{make_certificates}
\title{Generate certificates from a data frame}
\usage{
make_certificates(data, dir = ".", filename = NULL, quiet = FALSE, ...)
}
\arguments{
\item{data}{A data frame with one row per certificate.}

\item{dir}{Directory for the output PDFs. Created if it does not exist.}

\item{filename}{Optional character vector of file names, one per row. By
default names are derived from \code{recipient} (\code{"Jane A. Doe"} becomes
\code{"jane-a-doe.pdf"}), or taken from a \code{filename} column if \code{data} has
one. Duplicates get a numeric suffix so no file is silently overwritten.}

\item{quiet}{If \code{TRUE}, suppress the summary message.}

\item{...}{Arguments passed to \code{\link[=make_certificate]{make_certificate()}} for every row, such
as \code{logo}, \code{theme}, \code{signers}, \code{paper}, or \code{title}.}
}
\value{
A character vector of the paths written, invisibly.
}
\description{
Produces one PDF per row of \code{data}, for a class roster, an awards
ceremony, or any other list of recipients. Columns whose names match
arguments of \code{\link[=make_certificate]{make_certificate()}} are used row by row; anything constant
across the whole batch is passed through \code{...}.
}
\section{Columns}{

A \code{recipient} column is required unless \code{recipient} is supplied through
\code{...}. Any other column named after a \code{\link[=make_certificate]{make_certificate()}} argument is
used as well, so a spreadsheet with \code{recipient}, \code{award_name}, and
\code{citation} columns gives each person their own award and wording.
Columns that do not match an argument are ignored, and the ignored names
are reported so a misspelled heading is easy to spot. Empty cells (\code{NA})
fall back to whatever was passed in \code{...}, or to the default.

If a column and a \code{...} argument have the same name, the column wins,
since per-row values are the more specific of the two.
}

\examples{
roster <- data.frame(
  recipient  = c("Jane A. Doe", "John B. Smith"),
  award_name = c("Outstanding Student", "Outstanding Service"),
  citation   = c("for exceptional scholarship.",
                 "for years of devoted service.")
)

out <- file.path(tempdir(), "certificates")
files <- make_certificates(
  roster,
  dir          = out,
  organization = "University of Somewhere",
  signers      = list(
    list(name = "Alex Chair, Ph.D.", title = "Department Chair")
  ),
  device       = "pdf",
  quiet        = TRUE
)
basename(files)
}
