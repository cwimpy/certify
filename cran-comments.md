# Submission notes

This is a new submission.

## Test environments

* local macOS (R 4.x), R CMD check --as-cran: 0 errors, 0 warnings, 0 notes
  (excluding the standard "New submission" note).
* GitHub Actions: ubuntu-latest (R-release, R-devel, R-oldrel),
  windows-latest (R-release), macos-latest (R-release).
* R-hub: linux, windows-x86_64-devel, macos-arm64.

## R CMD check results

0 errors | 0 warnings | 0 notes

## Downstream dependencies

None — this is a new package.
